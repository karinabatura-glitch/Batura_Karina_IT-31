# Лекція 10. Відбір груп (HAVING)

*Лекція 9 навчила групувати рядки й агрегувати кожну групу окремо.
Але як відфільтрувати вже готові ГРУПИ — залишити лише власників із
більш ніж однією тваринкою, лише товари, які продали менше 5 разів?
`WHERE` тут не підходить: воно фільтрує рядки ДО групування, коли
агрегатне значення групи ще не існує. Для фільтрації груп ПІСЛЯ
агрегації є окрема команда — `HAVING`.*

## 1. Чому WHERE тут не спрацює

```sql
SELECT species, COUNT(*)
FROM animals
WHERE COUNT(*) > 1
GROUP BY species;
```

```text
OperationalError: misuse of aggregate: COUNT()
```

Це не дрібна синтаксична помилка, яку можна виправити — це
концептуальна невідповідність. Порядок виконання запиту (Лекції 3, 9):
`FROM → WHERE → GROUP BY → SELECT`. `WHERE` виконується ДО `GROUP BY`
— на цьому кроці груп ще не існує, а значить, і `COUNT(*)` для групи
ще нема з чим порівнювати. `WHERE` фільтрує **окремі рядки** за їхніми
власними значеннями, а не готові агрегати.

## 2. HAVING: фільтр після агрегації

```sql
SELECT species, COUNT(*) AS animals_count
FROM animals
GROUP BY species
HAVING COUNT(*) > 1;
```

`HAVING` — та сама ідея, що й `WHERE`, але на крок пізніше в конвеєрі:
`FROM → WHERE → GROUP BY → HAVING → SELECT`. `HAVING` виконується
ПІСЛЯ того, як групи вже сформовані й агрегатні функції для кожної з
них уже обчислені, тож у його умові можна вільно використовувати
`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`.

```mermaid
flowchart LR
    A["FROM + WHERE:\nфільтр окремих рядків"] --> B["GROUP BY:\nформування груп"]
    B --> C["Агрегатні функції\nобчислені для кожної групи"]
    C --> D["HAVING:\nфільтр ГОТОВИХ груп\nза агрегатним значенням"]
    D --> E["SELECT: підсумковий результат"]
```

## 3. WHERE і HAVING разом в одному запиті

```sql
SELECT owner_id, COUNT(*) AS animals_count
FROM animals
WHERE status = 'на обліку'
GROUP BY owner_id
HAVING COUNT(*) >= 2;
```

`WHERE` і `HAVING` чудово поєднуються — і означають дві принципово
різні речі: `WHERE status = 'на обліку'` прибирає окремі рядки тварин
не на обліку ще ДО групування (вони взагалі не потраплять у жодну
групу), а `HAVING COUNT(*) >= 2` після групування залишає лише тих
власників, у кого (серед тварин на обліку) їх двоє чи більше. Переплутати
їх легко: якщо написати умову на **рядок** у `HAVING` (коли вона мала
б бути у `WHERE`) — запит спрацює, але повільніше (фільтрація рядків
відкладена на потім); якщо написати умову на **агрегат** у `WHERE` —
запит одразу впаде з помилкою, як у розділі 1.

## 4. Псевдонім (AS) в умові HAVING

```sql
SELECT owner_id, COUNT(*) AS animals_count
FROM animals
GROUP BY owner_id
HAVING animals_count >= 2;
-- працює: HAVING виконується ПІСЛЯ того, як SELECT-псевдонім уже відомий
```

На відміну від `WHERE` (яке виконується до `SELECT` і тому не бачить
його псевдонімів — нагадування з Лекції 3), `HAVING` у SQLite може
посилатися на псевдонім, заданий через `AS` у `SELECT`, — це і зручно,
і водночас ще один нюанс поведінки, специфічний для SQLite: подвоєння
умови (`HAVING COUNT(*) >= 2` чи `HAVING animals_count >= 2`) дає
однаковий результат тут, але переносити такий запит "як є" на іншу
СУБД варто перевіряти окремо.

## Підсумок

- `WHERE` фільтрує окремі рядки ДО групування; `HAVING` фільтрує вже
  готові ГРУПИ ПІСЛЯ агрегації — саме тому лише в `HAVING` можна
  використовувати `COUNT`/`SUM`/`AVG`/`MIN`/`MAX` в умові.
- Повний порядок виконання запиту станом на цю лекцію:
  `FROM → WHERE → GROUP BY → HAVING → SELECT`.
- `WHERE` і `HAVING` можна використовувати в одному запиті одночасно —
  вони відсікають різні речі й не замінюють один одного.
- У SQLite `HAVING` може посилатися на псевдонім із `SELECT`
  (`AS ...`), на відміну від `WHERE`.

**Практика до цієї лекції:** [Практика 10. Відбір груп за допомогою HAVING](../practice/10-having.md)

## Рекомендована література

Харів Н. О. *Бази даних та інформаційні системи.* — Рівне: НУВГП (Національний університет водного господарства та природокористування), 2018 ([відкритий доступ](https://ep3.nuwm.edu.ua/9129/3/%D0%A5%D0%B0%D1%80%D1%96%D0%B2%20%D0%9D.%D0%9E.pdf)). Розділ SQL — фільтрація згрупованих даних (HAVING) як продовження теми GROUP BY.

Ужгородський національний університет, інженерно-технічний факультет (уклад.). *Організація баз даних: Робота з базами даних. Методичні вказівки і завдання до лабораторних робіт.* — Ужгород: ДВНЗ «Ужгородський національний університет», 2021 ([відкритий доступ](https://www.uzhnu.edu.ua/en/infocentre/get/42933)). Лабораторна робота 4 — умови над групами (HAVING) у складних запитах вибірки.
