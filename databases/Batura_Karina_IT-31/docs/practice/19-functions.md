# Практика 19. Написання функцій у PostgreSQL

*Мета заняття: написати дві власні функції мовою PL/pgSQL за Лекцією
20 для своєї бази в PostgreSQL — скалярну (з умовною логікою) і таку,
що повертає набір рядків, — і викликати обидві прямо в SQL.*

## Варіант

Ваша база у PostgreSQL з Практики 17 (перенесена схема й дані). Якщо
базу втрачено — поверніться до Практики 17.

## Приклад: наскрізна демонстрація

```sql
CREATE OR REPLACE FUNCTION age_category(age_years INTEGER)
RETURNS TEXT AS $$
BEGIN
    IF age_years < 1 THEN
        RETURN 'малюк';
    ELSIF age_years < 7 THEN
        RETURN 'дорослий';
    ELSE
        RETURN 'старший';
    END IF;
END;
$$ LANGUAGE plpgsql;

SELECT name, age, age_category(age) FROM animals;

CREATE OR REPLACE FUNCTION animals_by_species(p_species TEXT)
RETURNS TABLE(id INTEGER, name TEXT, age INTEGER) AS $$
BEGIN
    RETURN QUERY
    SELECT a.id, a.name, a.age FROM animals a WHERE a.species = p_species;
END;
$$ LANGUAGE plpgsql;

SELECT * FROM animals_by_species('кіт');
```

## Завдання

**Завдання 1. Скалярна функція з обчисленням.** Напишіть функцію, що
приймає значення числової колонки вашої таблиці (ціну, вік, стаж
тощо) і повертає похідне значення за формулою (аналогічно згенерованій
колонці з Лекції 17, але тепер як функція, а не колонка таблиці).
Викличте її в `SELECT` для всіх рядків таблиці.

**Завдання 2. Скалярна функція з умовною логікою.** Напишіть функцію
з `IF`/`ELSIF`/`ELSE`, що класифікує рядок вашої таблиці на 3+
категорії за значенням числової чи текстової колонки (наприклад,
"ціновий сегмент" за ціною, "стаж" за досвідом, "терміновість" за
датою). Викличте функцію в `SELECT` і письмово перевірте, що кожна
категорія коректно відповідає межам, які ви заклали в умовах.

**Завдання 3. Функція, що повертає таблицю.** Напишіть функцію з
одним параметром і `RETURNS TABLE(...)` + `RETURN QUERY`, що повертає
рядки вашої таблиці, відфільтровані за значенням параметра (аналог
`WHERE` із зовнішнім аргументом). Викличте її як `SELECT * FROM
ваша_функція(значення);` і письмово підтвердьте, що результат
відповідає прямому `SELECT ... WHERE ...` з тим самим значенням.

**Завдання 4. Свідома помилка: COMMIT у функції.** Напишіть (чи
змініть уже написану) функцію, що містить `COMMIT` чи `ROLLBACK`
усередині тіла, і спробуйте її викликати. Зафіксуйте письмово точний
текст помилки PostgreSQL і поясніть, чому саме функції заборонено
самій керувати транзакцією.

**Завдання 5. Функція в WHERE.** Складіть запит `WHERE`, що
використовує одну зі своїх функцій (Завдання 1 чи 2) прямо в умові
фільтрації (наприклад, `WHERE age_category(age) = 'старший'`).
Порівняйте письмово, наскільки такий запис читабельніший за розгортання
тієї самої умовної логіки прямо у `WHERE` через `CASE`.

## Що здати й оцінюється

Здайте `lab_work_19.sql` — усі три `CREATE FUNCTION` (Завдання 1-3) і
виклики `SELECT`, точний текст помилки з Завдання 4; і `lab_work_19.md`
— письмові пояснення до Завдань 2, 3 і 5. Контрольні питання:

- Чим `RETURNS TABLE(...)` принципово відрізняється від звичайного
  `RETURNS INTEGER`/`RETURNS TEXT` — що можна зробити з результатом
  в одному випадку, а що в іншому?
- Чому помилка з Завдання 4 виникає саме в момент ВИКЛИКУ функції
  (`SELECT ваша_функція(...)`), а не в момент її СТВОРЕННЯ
  (`CREATE FUNCTION`)?
- Яка функція з написаних (Завдання 1-3) могла би замінити подання з
  Практики 18, а яка — ні, і чому?

Оцінюється коректність логіки й типів усіх трьох функцій, правильно
зафіксована помилка з Завдання 4, і змістовність порівняння в
Завданні 5.

## Підсумок заняття

- Написано й перевірено три власні функції мовою PL/pgSQL для бази
  свого варіанта — скалярну, з умовною логікою, і таку, що повертає
  набір рядків.
- Використано власну функцію прямо в `SELECT` і `WHERE`, як
  вбудовану функцію SQL.
- На практиці підтверджено: функція не може сама виконати `COMMIT`/
  `ROLLBACK` — ця можливість зарезервована за процедурами (Лекція 21).

**Лекція до цієї практики:** [Лекція 20. Функції у PostgreSQL](../lectures/20-functions.md)

## Рекомендована література

Павловський В. І., Петрашенко А. В. *Бази даних та засоби управління.* — Київ: КПІ ім. Ігоря Сікорського, 2024 ([відкритий доступ](https://ela.kpi.ua/bitstreams/9f26675d-c42f-485e-aab5-13e6e014b560/download)). Розділ «PostgreSQL і PL/pgSQL» — створення функцій мовою PL/pgSQL.

Павловський В. І., Петрашенко А. В., Побєда Д. В. *Бази даних та засоби управління. Практикум.* — Київ: КПІ ім. Ігоря Сікорського, 2021 ([відкритий доступ](https://ela.kpi.ua/server/api/core/bitstreams/1b620029-0db7-4e2e-83e7-32f7722a69be/content)). Розділ про збережені процедури та тригери — створення серверних функцій (server-side function creation).
